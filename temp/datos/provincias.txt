ArrayList<String> ubicaciones;
        Map<String, ArrayList<String>> provincias = new HashMap<>();
        
        
        for(String provincia : ubicaciones) {
            
            String[] provinceParts = provinca.split(",");
            ArrayList<String> provincasPut = new ArrayList<>();
            provincias.putIfAbsent(provinceParts[0], new ArrayList<>());
            provincasPut = provincias.get(provinceParts[0]);
            provincasPut.add(provinceParts[1] + "," + provinceParts[2] + "," + provinceParts[3]);
        }